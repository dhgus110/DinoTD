# Dino TD 게임 기획 협업 지침

## 작업 범위

- 모든 작업은 `C:\Users\jeong\Desktop\DinoTD`를 프로젝트 루트로 삼는다.
- 파일을 생성하거나 변경할 때는 이 작업 루트 안에서만 수행한다.
- 기존 파일을 덮어쓰기 전에는 현재 내용과 변경 범위를 먼저 확인한다.
- 이 문서는 협업 방식의 원본이며, 세부 게임 기획 작성 규칙의 원본은 `docs/guidelines/game-design-authoring-rules.md`이다.

## 기본 언어와 표현

- 사용자와의 대화 및 기획 문서는 한국어를 기본으로 작성한다.
- 코드, 식별자, 파일명, 고유명사처럼 정확성이 필요한 경우에만 영어를 사용하거나 병기한다.
- 모호한 표현보다 조건, 결과, 예외와 검증 방법이 드러나는 표현을 사용한다.
- 대화에서 새로운 기획 용어, 기술 용어 또는 영어 약어를 처음 사용할 때는 반드시 바로 풀어서 설명한다.
- 설명은 `쉬운 한국어 의미 → Dino TD 안의 구체적인 예시 → 해당 결정이 기획·구현에 미치는 영향` 순서로 한다.
- 쉬운 한국어만으로 충분히 설명할 수 있으면 전문 용어를 먼저 사용하지 않는다.
- 사용자가 이미 아는 용어라고 임의로 가정하지 않는다.

## 게임 기획 대화 흐름

게임 기획 작업은 다음 순서로 진행한다.

1. 사용자의 현재 목표와 작업 루트에 있는 기존 자료를 확인한다.
2. 작성하려는 기획서에 필요한 전체 규칙 항목을 먼저 목록으로 제시한다.
3. 규칙 사이의 의존 관계와 결정 순서를 정하고, 최우선 규칙부터 논의한다.
4. 진행에 필요한 핵심 질문을 한 번에 소수씩 제시한다.
5. 답변과 제안을 `확정`, `가정`, `미정`, `검증 필요`로 구분한다.
6. 관련 규칙의 정상 상황, 예외 상황, 실패 상황과 종료 처리가 모두 합의됐는지 확인한다.
7. 사용자와 합의되어 `확정`된 내용만 기준 문서에 반영한다.
8. 각 작업이 끝날 때 변경사항과 남은 결정을 간결하게 요약한다.

브레인스토밍 내용과 확정된 기획을 섞지 않는다. 논의를 위한 제안은 제안임을 명시하고, 사용자의 승인 전에는 확정 사항처럼 기록하지 않는다.

## 기획서 작성 전 규칙 확정

- 세부 기획서를 바로 작성하기 전에 해당 시스템의 전체 규칙 지도를 먼저 만든다.
- 전체 규칙 지도에는 최소한 목적, 시작 조건, 진행 순서, 플레이어 선택, 비용과 보상, 제한, 우선순위, 종료 조건, 실패 처리, 저장·복원, UI 피드백과 검증 항목을 포함한다.
- 규칙 지도를 만들 때는 기존 문서와 충돌하거나 비어 있는 항목을 표시하고 사용자와 결정 순서를 합의한다.
- 규칙 하나를 확정하기 전에 그 규칙이 전투, 카드, 맵, 경제, UI, 저장과 분석에 주는 영향을 확인한다.
- 아직 합의하지 않은 규칙은 기획서 본문에 확정 문장으로 작성하지 않는다.
- 전체 규칙이 모두 확정되기 전에도 개발 차단을 막기 위한 문서를 만들 수 있지만, 이 경우 확정 범위와 미정 범위를 문서 상단에 분명히 표시한다.

## 결정과 제안 원칙

- 사용자 승인 없이 핵심 게임 규칙, 수치, 장르, 플랫폼, 비즈니스 모델(BM), 대상 이용자를 확정하지 않는다.
- 중요한 정보가 부족하면 임의로 채우지 말고 질문하거나 `가정`, `미정`, `검증 필요`로 표시한다.
- 여러 선택지가 있으면 각 선택지의 의미, 장점, 단점을 간결하게 비교하고 추천안과 추천 이유를 함께 제시한다.
- 추천안 역시 사용자가 승인하기 전에는 `확정`으로 취급하지 않는다.
- 이미 확정된 사항을 변경해야 한다면 변경 이유와 영향을 먼저 설명하고 다시 합의한다.

## 결정 상태 정의

- `확정`: 사용자가 승인하여 이후 기획과 구현의 기준으로 사용할 수 있는 내용.
- `가정`: 논의를 진행하기 위해 임시로 채택했으며 추후 확인이 필요한 내용.
- `미정`: 아직 선택하거나 정의하지 않은 내용.
- `검증 필요`: 플레이테스트, 데이터, 기술 조사 등 근거를 통해 확인해야 하는 내용.

## 문서와 정보 관리

- 같은 규칙, 수치, 용어는 하나의 원본에서만 관리한다.
- 다른 문서에서 같은 정보가 필요하면 내용을 복제하지 말고 원본 문서나 해당 절을 참조한다.
- 기획 문서를 작성하거나 검토하기 전에 `docs/guidelines/game-design-authoring-rules.md`에서 작업과 관련된 절을 확인하고 따른다.
- `docs/guidelines/game-design-authoring-rules.md` 전체를 매 작업마다 불필요하게 읽거나 다른 문서에 복제하지 않는다.
- 구체적인 게임 기획서 파일 구조와 템플릿은 별도의 합의를 거쳐 정한다.

## 작업 완료 보고

작업 완료 시 다음 내용을 알린다.

- 생성하거나 변경한 파일
- 반영한 `확정` 사항
- 사용한 `가정`과 `검증 필요` 사항
- 아직 남아 있는 `미정` 사항 또는 다음 결정
